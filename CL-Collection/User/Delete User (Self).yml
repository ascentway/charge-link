info:
  name: Delete User (Self)
  type: http
  seq: 11

http:
  method: DELETE
  url: http://localhost:8080/api/v1/auth/me
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
